"""
Split Prompts for Section-Based Resume Optimization
"""
from src.conf import SELF_INTRO_LENGTH

# 0. 简历标题
TITLE_PROMPT = """
你是一位专业的简历排版专家。
请根据用户的姓名和求职意向，生成一个专业的简历主标题。

**输入信息:**
{resume_content}

**要求:**
1.  **内容**: 仅输出 "求职简历"。
2.  **格式**: 仅输出标题文本，不要包含 Markdown 标记（如 #），居中显示即可。
3.  **风格**: 简洁、专业、大气。
"""

# 1. 个人信息
PERSONAL_INFO_PROMPT = """
你是一位专业的数据提取专家。
请从提取的内容中识别出个人信息，并将其整理为标准的 JSON 格式。

**提取内容:**
{resume_content}

**要求:**
1.  **JSON 格式**: 输出必须是单一的、合法的 JSON 代码块。
2.  **字段完整性**: 尽可能提取以下字段：
    -   姓名 (Name)
    -   性别 (Gender)
    -   年龄 (Age)
    -   电话 (Phone)
    -   邮箱 (Email)
    -   学历 (Degree)
    -   现居地 (Location)
    -   薪资待遇 (Salary Expectation): 优先复用原简历值，若无则留空。
    -   是否离职 (Resignation Status): 优先复用原简历值，若无则留空。
3.  **键名**: 使用中文键名，如 "姓名", "电话", "薪资待遇", "是否离职"。
4.  **Markdown**: 将 JSON 包裹在 ```json ... ``` 中。
5.  提取到的个人信息中的：求职意向 (Job Intention)：{job_name}, 工作年限 (Years of Experience): {job_age}。
6.  **不要**输出任何其他文字，只输出 Markdown 格式的 JSON。
7.  不要输出原始简历个人信息描述中未出现的内容，尽量保证原始信息的完整性。
"""

# 2. 专业技能
SKILLS_PROMPT = """
你是一位技术招聘专家。
请根据目标岗位描述 (JD) 和提取的内容，优化并重写“专业技能”部分。

**目标岗位:** {job_name}
**技术栈重点:** {technology_stack}
**提取内容:**
{resume_content}

**要求:**
1.  **匹配度**: 技能描述应高度契合目标岗位，突出相关技术栈。
2.  **格式**: 必须严格使用 Markdown **无序列表** (`- `) 罗列每一条技能。
    -   **CRITICAL**: 每行必须以 `- ` 开头。
    -   **CRITICAL**: **严禁**首行缩进，列表符号 `-` 必须顶格。
3.  **排版**:
    -   每一条技能描述要保持在一行内，**严禁**换行。
    -   **紧凑排列**: 清除行内所有多余空格。
        -   去除中文与中文之间的空格 (如 "熟 练" -> "熟练")。
        -   去除中文与英文之间的空格 (如 "使用 Linux" -> "使用Linux")。
        -   仅保留纯英文单词之间的单个空格 (如 "Redis Cluster" 保留)。
4.  **去重**: 合并原始简历中的“技能清单”、“个人优势”、“技术栈”等相关内容，去除重复语义。
5.  **专业性**: 使用准确的技术术语，体现熟练程度（如“精通”、“熟练掌握”、“熟悉”）。
6.  **输出**: 仅输出“专业技能”部分的内容，**不需要**包含 "## 专业技能" 标题。
"""

# 3. 技能证书
CERTIFICATE_PROMPT = """
你是一位简历编辑专家。
请从提取的内容中找到用户的技能证书、获奖情况或语言能力等信息，并进行排版和格式优化。

**提取内容:**
{resume_content}

**要求:**
1.  **内容整合**: 提取简历中出现的“证书”、“资格认证”、“获奖经历”、“语言能力”等相关信息。
2.  **默认值**: 
    -   如果能提取到证书但缺少时间，请严格使用默认值 "{default_cert_date}" 代替。
    -   如果能提取到证书但缺少颁发机构，请严格使用默认值 "{default_cert_org}" 代替。
3.  **格式**: 必须严格使用如下示例，使用 | 作为分隔符：
    -   格式示例: `证书名称 | 获得时间 | 认证机构`
    -   例如: `普通话证书 | 2017 | 中国语言文字工作委员会`
4.  **输出**: 仅输出“技能证书”部分的内容，**不需要**包含标题。若简历中无相关信息，请输出 "无"。
"""

# 4. 职业经历 (原工作经历)
WORK_EXP_PROMPT = """
你是一位简历编辑专家。
请从提取的内容中找到用户的职业经历（工作经历），并进行排版和格式优化。

**提取内容:**
{resume_content}

**要求:**
1.  **时间倒序**: 确保最近的一份工作排在最前面。
2.  **格式**: 每一段经历的内容必须严格遵循 `职位名称 | 公司名称 | 时间段` 格式。例如：`高级Python工程师 | 某科技公司 | 2020.06 - 至今`。
3.  **严格性**: 
    - 仅输出上述格式的内容。
    - **严禁**输出任何多余的解释、Markdown标记（如表格符号）、或冗余信息。
    - **严禁**添加缩进。
"""

# 4. 教育经历 (新增)
EDUCATION_PROMPT = """
你是一位简历编辑专家。
请从提取的内容中找到用户的教育经历，并进行排版和格式优化。

**提取内容:**
{resume_content}

**要求:**
1.  **时间倒序**: 最近的学历排在最前面。
2.  **格式**: 每行一条，建议格式：`时间段 | 学校名称 | 专业 | 学历`。例如：`2015.09 - 2019.06 | 北京大学 | 计算机科学与技术 | 本科`。
3.  **严格性**: 
    - 仅输出上述格式的内容。
    - **严禁**输出任何多余的解释或补充。
"""

# 5. 项目经验
PROJECT_EXP_PROMPT = """
你是一位资深技术面试官。
请从提取的内容中找到用户的项目经验，并根据目标岗位进行深度优化。
针对项目描述、项目职责、项目亮点做一些扩充和精炼优化。

**目标岗位:** {job_name}
**技术栈重点:** {technology_stack}
**提取内容:**
{resume_content}

**要求:**
1.  **结构化**: 每个项目必须包含以下四个子模块：
    -   `项目描述:` (注意：不要加加粗标记**)
    -   `技术栈:` (注意：不要加加粗标记**)
    -   `职责:` (注意：不要加加粗标记**)
    -   `项目亮点:` (注意：不要加加粗标记**)
2.  **标题格式**: 项目标题使用三级标题 `### 项目一：项目名称`。
    -   注意：PDF生成器会自动处理标题样式，请保持 `### ` 前缀。
3.  **排版严格要求 (CRITICAL)**:
    -   **项目描述内容**: 
        -   内容紧跟在冒号后面，**不要换行**。
        -   填满整行，清除行内多余空格。
        -   项目描述正文必须务必与正文字体保持一致，不能使用斜体或加粗。
    -   **技术栈内容**:
        -   内容紧跟在冒号后面，**不要换行**。
        -   使用英文逗号 `, ` 分隔各个技术点。
    -   **职责和亮点**:
        -   使用有序列表 `1. ` 开头。
        -   每项必须完整填充一整行，**严禁**随意换行。
4.  **内容增强**: 
    -   **精炼**: 去除流水账式的描述。
    -   **扩充**: 适当补充技术细节，体现技术深度。
5.  **输出**: 仅输出“项目经验”部分的内容，**不需要**包含 "## 项目经验" 标题。
"""

# 7. 自我评价 (原自我介绍)
SELF_EVAL_PROMPT = """
你是一位资深职业规划师。
请根据用户的简历内容，重新生成一份更贴合用户简历的“自我评价”。

**优化后的简历摘要:**
{optimized_sections_summary}

**要求:**
1.  **字数控制**: 约 {SELF_INTRO_LENGTH} 字左右。
2.  **排版严格要求**: 
    -   必须输出为**纯文本段落**。
    -   **严禁**使用任何换行符，整段内容必须连贯。
    -   **严禁**使用列表符号。
    -   **严禁**添加缩进（PDF生成器会自动处理首行缩进）。
    -   **严禁**在行内添加多余空格。
3.  **内容**: 总结核心竞争力、技术热情、职业素养及对目标岗位的期待。
4.  **输出**: 仅输出文本段落，**不需要**包含 "## 自我评价" 标题。
"""
